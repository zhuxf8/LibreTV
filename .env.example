# 必填：访问密码（不设置时站点会提示管理员配置后再可用）
PASSWORD=change-me

# 可选：会话/代理签名密钥；不设置时从 PASSWORD 派生
# PROXY_SECRET=

# 可选：代理请求上游的超时与重试
REQUEST_TIMEOUT=8000
MAX_RETRIES=1

# 可选：调试
DEBUG=false

# 可选：直接使用发布镜像（如 ghcr.io/xxx/libretv:2.0.0）时设置，
# 不设置则用本地源码构建（tag 与 package.json 的 version 对应）
# LIBRETV_IMAGE=
